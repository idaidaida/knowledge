<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title th:text="${post.title != null ? post.title : '記事'}">記事</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-900">

<header class="sticky top-0 z-10 bg-white/90 backdrop-blur border-b border-gray-200">
  <div class="mx-auto max-w-md w-full px-4 h-12 flex items-center justify-between">
    <a href="/" class="text-sm text-blue-600">ホーム</a>
    <h1 class="text-base font-semibold">記事</h1>
    <div class="flex items-center gap-3">
      <div class="relative">
        <button id="notifyBtn" type="button" class="p-2 rounded-md text-gray-600 hover:bg-gray-100 relative" aria-haspopup="true" aria-expanded="false">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5">
            <path d="M12 2.25a3.75 3.75 0 00-3.75 3.75v1.098c0 .414-.168.812-.467 1.102A6.724 6.724 0 006 13.5v.75h12v-.75a6.724 6.724 0 00-1.783-5.3 1.5 1.5 0 01-.467-1.102V6A3.75 3.75 0 0012 2.25z"/>
            <path d="M8.25 16.5h7.5a3.75 3.75 0 11-7.5 0z"/>
          </svg>
          <span th:if="${notificationCount} > 0" class="absolute -top-0.5 -right-0.5 min-w-[1rem] h-4 px-1 rounded-full bg-red-600 text-white text-[10px] leading-4 text-center" th:text="${notificationCount}">0</span>
        </button>
        <div id="notifyMenu" class="hidden absolute right-0 mt-1 w-72 max-h-80 overflow-auto bg-white border border-gray-200 rounded-md shadow-lg p-2">
          <div class="text-xs text-gray-500 mb-1">通知</div>
          <div th:if="${#lists.isEmpty(notifications)}" class="text-sm text-gray-600 p-2">新しい通知はありません</div>
          <a th:each="n : ${notifications}"
             th:href="@{${n.kind} == 'POST' ? '/posts/' + ${n.refId} : '/posts/' + ${n.postId}}"
             class="block px-2 py-1.5 rounded hover:bg-gray-50">
            <span class="text-xs text-gray-500" th:text="${#dates.format(n.createdAt, 'MM/dd HH:mm')}">00/00 00:00</span>
            <div class="text-sm text-gray-900" th:text="${n.kind} == 'POST' ? ('新しい投稿: ' + (${n.title} ?: ${#strings.abbreviate(n.body, 24)})) : ('新しいコメント: ' + ${n.actor})">通知</div>
          </a>
          <form method="post" action="/notifications/seen" class="mt-2 text-right">
            <button type="submit" class="text-xs text-blue-600 underline underline-offset-2">すべて既読</button>
          </form>
        </div>
      </div>
      <a href="/logout" class="text-sm text-gray-600">ログアウト</a>
    </div>
  </div>
</header>

<main class="mx-auto max-w-md w-full px-4 pb-28 pt-3">

  <article class="relative rounded-xl bg-white border border-gray-200 shadow-sm p-3">
    <div th:if="${session.loginUser} == ${post.username}" class="absolute top-2 right-2">
      <button type="button" id="postMenuBtn" aria-haspopup="true" aria-expanded="false"
              class="p-2 rounded-md text-gray-500 hover:bg-gray-100 focus:outline-none">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5">
          <path d="M12 6.75a1.125 1.125 0 1 0 0-2.25 1.125 1.125 0 0 0 0 2.25Z"/>
          <path d="M12 13.125a1.125 1.125 0 1 0 0-2.25 1.125 1.125 0 0 0 0 2.25Z"/>
          <path d="M13.125 18a1.125 1.125 0 1 1-2.25 0 1.125 1.125 0 0 1 2.25 0Z"/>
        </svg>
      </button>
      <div id="postMenu" class="hidden absolute right-0 mt-1 w-28 rounded-md border border-gray-200 bg-white shadow-lg py-1 text-sm">
        <a th:href="@{'/posts/' + ${post.id} + '/edit'}" class="block px-3 py-1.5 text-gray-700 hover:bg-gray-50">編集</a>
        <form th:action="@{'/posts/' + ${post.id} + '/delete'}" method="post">
          <button type="submit" class="block w-full text-left px-3 py-1.5 text-gray-700 hover:bg-gray-50">削除</button>
        </form>
      </div>
    </div>
    <img th:if="${!#strings.isEmpty(post.imageUrl)}" th:src="${post.imageUrl}"
         alt="cover" class="w-full max-h-80 object-cover rounded-md bg-gray-100" />

    <div class="mt-2 text-xs text-gray-500">
      <a th:href="@{'/users/' + ${post.username}}" th:text="${post.username}" class="underline underline-offset-2">username</a>
      <span class="mx-1">·</span>
      <span th:text="${#dates.format(post.createdAt, 'yyyy-MM-dd HH:mm')}"></span>
    </div>

    <h2 th:if="${post.title}" th:text="${post.title}" class="mt-1 text-lg font-semibold"></h2>
    <h2 th:if="${post.title == null}" class="mt-1 text-lg font-semibold">記事</h2>
    <div class="mt-2 whitespace-pre-line text-[15px] text-gray-800" th:text="${post.content}"></div>

    <div class="mt-3 flex items-center gap-4">
      <a th:href="@{'/posts/' + ${post.id} + '/go'}" target="_blank" rel="noopener" class="inline-flex items-center gap-1 text-sm text-blue-600 underline underline-offset-2">ニュースを開く</a>
      <span class="text-xs text-gray-600">いいね: <span th:text="${likeCount}">0</span></span>
      <form th:if="${likedByMe}" th:action="@{'/posts/' + ${post.id} + '/unlike'}" method="post" class="inline">
        <button type="submit" class="text-xs text-gray-500 underline underline-offset-2 bg-transparent p-0">いいね解除</button>
      </form>
      <form th:if="${!likedByMe}" th:action="@{'/posts/' + ${post.id} + '/like'}" method="post" class="inline">
        <button type="submit" class="text-xs text-gray-500 underline underline-offset-2 bg-transparent p-0">いいね</button>
      </form>
    </div>

  </article>

  <section class="mt-4">
    <h3 class="text-sm font-semibold text-gray-700">コメント</h3>
    <div th:if="${#lists.isEmpty(comments)}" class="text-sm text-gray-500 py-3">まだコメントはありません</div>

    <div th:each="c : ${comments}" class="border-b border-gray-200 py-3">
      <div class="min-w-0">
        <div class="text-xs text-gray-500">
          <span class="font-medium text-gray-700" th:text="${c.username}"></span>
          <span class="mx-1">·</span>
          <span th:text="${#dates.format(c.createdAt, 'yyyy-MM-dd HH:mm')}"></span>
        </div>
        <div class="mt-1 whitespace-pre-line text-[15px] text-gray-800" th:text="${c.content}"></div>
        <div class="mt-1 text-xs" th:if="${session.loginUser} == ${c.username}">
          <a th:href="@{'/comments/' + ${c.id} + '/edit'}" class="text-gray-500 underline underline-offset-2">編集</a>
          <form th:action="@{'/comments/' + ${c.id} + '/delete'}" method="post" class="inline">
            <button type="submit" class="text-red-600 ml-3">削除</button>
          </form>
        </div>
      </div>
    </div>

    <div th:if="${session.loginUser} == null" class="mt-4 rounded-xl bg-white border border-gray-200 shadow-sm p-3 text-center">
      <p class="text-sm text-gray-700">コメントするにはログインしてください。</p>
      <a href="/login" class="mt-2 inline-flex items-center rounded-md bg-blue-600 px-4 py-2 text-sm font-medium text-white shadow-sm active:bg-blue-700">ログインへ</a>
    </div>

    <div th:if="${session.loginUser} != null" class="mt-4">
      <button type="button" id="openCommentModal"
              class="inline-flex items-center rounded-md bg-blue-600 px-3 py-2 text-sm font-medium text-white shadow-sm active:bg-blue-700">
        コメントする
      </button>
    </div>

    <!-- Comment Modal -->
    <div th:if="${session.loginUser} != null" id="commentModal" class="fixed inset-0 z-30 hidden" aria-hidden="true">
      <div class="absolute inset-0 bg-black/40" id="commentModalBackdrop"></div>
      <div class="absolute inset-x-0 bottom-0 sm:inset-0 sm:flex sm:items-center sm:justify-center">
        <div class="sm:my-8 sm:max-w-md sm:w-full bg-white rounded-t-2xl sm:rounded-2xl shadow-xl border border-gray-200">
          <div class="p-4 border-b border-gray-200 flex items-center justify-between">
            <h4 class="text-sm font-semibold">コメントを投稿</h4>
            <button type="button" id="closeCommentModal" class="text-gray-500 text-sm">閉じる</button>
          </div>
          <form method="post" action="/comments" class="p-4 space-y-2">
            <input type="hidden" name="postId" th:value="${post.id}" />
            <div class="text-xs text-gray-600">コメント投稿者: <span class="font-medium text-gray-800" th:text="${session.loginUser}"></span></div>
            <label class="block text-sm text-gray-700 mb-1">コメント（改行のみ対応）</label>
            <textarea name="content" required placeholder="コメントを入力..."
                      class="w-full h-32 resize-y rounded-md border border-gray-300 px-3 py-2 text-[15px] focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
            <div class="flex justify-end gap-3 pt-1">
              <button type="button" id="cancelCommentModal" class="text-sm text-gray-600 underline underline-offset-2">キャンセル</button>
              <button type="submit" class="inline-flex items-center rounded-md bg-blue-600 px-3 py-2 text-sm font-medium text-white shadow-sm active:bg-blue-700">投稿</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </section>

</main>

<nav class="fixed bottom-0 inset-x-0 z-20 bg-white/95 backdrop-blur border-t border-gray-200">
  <div class="mx-auto max-w-md w-full px-8 h-14 grid grid-cols-3 items-center text-xs text-gray-600">
    <a href="/" class="justify-self-start inline-flex items-center gap-2">
